# OpenLongTail WM 1.3B — inference dependencies.
# Captured from the working release environment (Python 3.11, CUDA 12.8, H200).
#
# torch/torchvision carry a +cu128 local tag; install them from the CUDA-12.8
# wheel index (see README) if plain `pip install` cannot resolve them:
#   pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu128
torch==2.8.0
torchvision==0.23.0

# diffusion / model stack
diffusers==0.39.0
transformers==5.14.1
tokenizers==0.22.2
accelerate==1.14.0
huggingface-hub==1.27.0
safetensors==0.8.0
einops==0.8.2
ftfy==6.3.1
easydict==1.13

# video / image io  (pyav backend is required for the 41-frame mp4 contract)
imageio==2.37.4
imageio-ffmpeg==0.6.0
av==18.0.0
pillow==12.2.0
opencv-python-headless==4.11.0.86

# numerics
numpy==1.26.4
scipy==1.17.1
tqdm==4.70.0

# ---------------------------------------------------------------------------
# NOT pinned here (only needed for the RAW tier — your own dashcam video):
#   * DepthAnythingV2 ships inside `transformers` above (no extra install).
#   * MapAnything (facebook/map-anything-apache) has its own heavy deps and is
#     best kept in a SEPARATE venv; point scripts at it with
#     OLT_MAPANYTHING_PYTHON=/path/to/mapanything_venv/bin/python.
#     See https://github.com/facebookresearch/map-anything
#   * Qwen2.5-VL captioner also loads via `transformers`; large download.
# The CACHED tier (the 3 bundled demo clips) needs none of these.
